// Tipos específicos para {{Name}}
export interface {{Name}} {
  id: string;
  fecha_creacion: string;
  // Agregar otros campos específicos aquí
}

// Tipos adicionales si son necesarios
export interface {{Name}}FormData {
  // Campos del formulario
}